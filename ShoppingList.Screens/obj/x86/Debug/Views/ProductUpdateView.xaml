<UserControl
    x:Class="ShoppingList.Screens.Views.ProductUpdateView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ShoppingList.Screens.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="400"
    d:DesignWidth="400">

    <Grid Background="White">

        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Button Content="ADD NEW PRODUCT" 
                Grid.Row="1"
                HorizontalAlignment="Right"
                Margin="10 0"
                Style="{StaticResource NavigationButton}"/>

        <StackPanel Orientation="Horizontal" 
                    VerticalAlignment="Bottom"
                    Grid.Row="1">
            <TextBox Text="Product" Style="{StaticResource ProductTitleTextBox}"/>
            <TextBlock Text="05/12/2022" Style="{StaticResource ProductDataRegister}"/>
        </StackPanel>

        <TextBox Text="Lorem Ipsum has been the industry's standard dummy text ever since the 1500s,  when an unknown printer took a galley of type and scrambled it to make a  type specimen book." 
                   Style="{StaticResource ProductDescriptionTextBox}"
                   Grid.Row="2"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal">
            <Button x:ConnectionId='2' Name="SeeAllProductsButton" 
                    Content="See all Products" 
                    Style="{StaticResource NavigationButton}"
                    />
            <Button x:ConnectionId='3' Name="RegisterProductButton" 
                    Content="Register Product" 
                    Style="{StaticResource NavigationButton}"
                    />
        </StackPanel>

    </Grid>
    
</UserControl>

